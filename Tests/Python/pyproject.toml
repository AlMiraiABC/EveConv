[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10,<3.13"
dependencies = [
    # "cuda-toolkit>=13.1.0",
    "dotenv>=0.9.9",
    "hf-xet>=1.2.0",
    "huggingface-hub>=0.36.0",
    "numpy<2.0.0",          # torch doesn't support numpy>=2.0.0 yet
    "torch>=2.9.1",
    "transformers>=4.57.3",
]

[[tool.uv.index]]
url = "https://pypi.tuna.tsinghua.edu.cn/simple"
default = true

# GPU
# [[tool.uv.index]]
# name = "pytorch-cu128"
# url = "https://download.pytorch.org/whl/cu128"
# explicit = true

# [tool.uv.sources]
# torch = [
#   { index = "pytorch-cu128", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
# ]
